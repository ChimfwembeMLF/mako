# Production environment template — resources/client
# Optional: copy to .env.production.local before building locally.
# deploy:prod / yarn build uses build:prod (same-origin, empty API base URL).

# Same-origin — leave empty; browser uses window.location.origin
VITE_API_BASE_URL=

# Embeddable chatbot widget — must match MAKO_WIDGET_API_KEY in autopilot-api .env
VITE_WIDGET_API_KEY=pk_live_5dcdd3fa_d6868822cc6b90006b2d7b6e274de67f4c48
