/** Start auto-renew charge this many hours before billing period ends. */
export const RENEWAL_WINDOW_HOURS = 24;

/** Grace period after expiry before features are blocked. */
export const RENEWAL_GRACE_DAYS = 3;

/** Max failed renewal attempts before auto-renew is disabled. */
export const MAX_RENEWAL_ATTEMPTS = 3;
