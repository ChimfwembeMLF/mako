{
  "name": "autopilot-widget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "WIDGET_ENTRY=loader vite build && WIDGET_ENTRY=avatar-3d vite build && WIDGET_ENTRY=ar-view vite build && node scripts/copy-to-client.mjs",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "dompurify": "^3.4.0",
    "marked": "^15.0.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
