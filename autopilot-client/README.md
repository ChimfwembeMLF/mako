# Moved

The React client now lives at:

```
autopilot-api/resources/client/
```

Start dev from repo root:

```bash
yarn dev
```

Or from `autopilot-api/`:

```bash
yarn dev
```

Open http://localhost:3000
